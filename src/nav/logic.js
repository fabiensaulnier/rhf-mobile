import { createLogic } from 'redux-logic';

// const submitLocationLogic = createLogic({
//   type: SUBMIT_LOCATION,
//   latest: true,
//   validate({ getState, action }, allow, reject) {
//     if (getState().order.createForm.values.location.address) {
//       allow(action);
//     } else {
//       reject();
//     }
//   },
// });

export default [];
